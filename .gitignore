# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node / Next ---
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# --- Env files ---
.env
.env.local
.env.*.local
!.env.example

# --- Storage / uploads ---
backend/storage/
backend/.uploads/
# When uvicorn is launched with --app-dir backend from the repo root
# (e.g. via .claude/launch.json), the runtime upload dir lands at the
# repo root instead of under backend/. Ignore that one too.
/storage/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Claude / agent scratch ---
.claude/
.claude/
